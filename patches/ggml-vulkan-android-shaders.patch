*** Begin Patch
*** Update File: ggml/src/ggml-vulkan/CMakeLists.txt
@@ -6,7 +6,11 @@
     cmake_policy(SET CMP0147 NEW)
 endif()
 
-find_package(Vulkan COMPONENTS glslc REQUIRED)
+if (ANDROID)
+    message(STATUS "Android: Using runtime Vulkan shaders (no glslc)")
+else()
+    find_package(Vulkan COMPONENTS glslc REQUIRED)
+endif()
 
 if (DEFINED ENV{VULKAN_SDK})
     list(APPEND CMAKE_PREFIX_PATH "$ENV{VULKAN_SDK}")
@@ -14,7 +18,13 @@
 find_package(SPIRV-Headers CONFIG REQUIRED)
 
 if (Vulkan_FOUND)
-    message(STATUS "Vulkan found")
+    if (ANDROID)
+        message(STATUS "Vulkan found (Android: runtime shader compilation)")
+        add_compile_definitions(GGML_VULKAN_RUNTIME_SHADERS)
+    else()
+        message(STATUS "Vulkan found")
+    endif()
 
     ggml_add_backend_library(ggml-vulkan
                              ggml-vulkan.cpp
@@ -22,8 +32,10 @@
                             )
 
     set(VULKAN_SHADER_GEN_CMAKE_ARGS "")
 
+if (NOT ANDROID)
     # Test all shader extensions
     test_shader_extension_support(
         "GL_KHR_cooperative_matrix"
         "${CMAKE_CURRENT_SOURCE_DIR}/vulkan-shaders/feature-tests/coopmat.comp"
@@ -97,7 +109,7 @@
         GGML_VULKAN_BFLOAT16_GLSLC_SUPPORT
     )
 
-    target_link_libraries(ggml-vulkan PRIVATE Vulkan::Vulkan)
+    target_link_libraries(ggml-vulkan PRIVATE Vulkan::Vulkan SPIRV-Headers::SPIRV-Headers)
     target_include_directories(ggml-vulkan PRIVATE ${CMAKE_CURRENT_BINARY_DIR})
 
     # Workaround to the "can't dereference invalidated vector iterator" bug in clang-cl debug build
@@ -151,6 +163,7 @@
         set(HOST_CMAKE_TOOLCHAIN_FILE "")
     endif()
 
+    # Pre-compile shaders from .comp → .cpp (skipped on Android: runtime compilation)
     include(ExternalProject)
     if (CMAKE_CROSSCOMPILING)
         list(APPEND VULKAN_SHADER_GEN_CMAKE_ARGS -DCMAKE_TOOLCHAIN_FILE=${HOST_CMAKE_TOOLCHAIN_FILE})
@@ -225,6 +238,7 @@
                     vulkan-shaders-gen
             COMMENT "Generate vulkan shaders for ${file}"
         )
+    endif()  # NOT ANDROID
         target_sources(ggml-vulkan PRIVATE ${_ggml_vk_target_cpp})
     endforeach()
 
*** End Patch
